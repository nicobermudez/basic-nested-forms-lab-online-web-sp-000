<%= form_for @recipe do |f| %>
  <%= f.label :title %>
  <%= f.text_field :name %><br>

  <%= fi.fields_for :ingredients do |ingredient| %>
    <%= ingredient.label :title %>
    <%= ingredient.text_field :name %><br>

    <%= ingredient.label :quantity %>
    <%= ingredient.text_field :quantity %>
  <% end %>
  <%= f.submit %>
<% end %>
